import { SnappyChunk } from './dechunk';
import { IwaObject, Registry } from './split';
export interface ArchiveEntryBase {
    type: 'file' | 'iwa';
    name: string;
}
export interface File extends ArchiveEntryBase {
    type: 'file';
    data: Uint8Array;
}
export interface IwaArchive extends ArchiveEntryBase {
    type: 'iwa';
    chunks: IwaChunk[];
}
export interface IwaChunk extends SnappyChunk {
    objects: IwaObject[];
}
export type ArchiveEntry = File | IwaArchive;
export declare function decode(data: Uint8Array, registry: Registry): AsyncIterableIterator<ArchiveEntry>;
//# sourceMappingURL=decode.d.ts.map